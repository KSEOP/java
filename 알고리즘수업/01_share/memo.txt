- 문호선(010-3126-3501, moonhosun@gmail.com)
- 공유폴더: naver.me/5P2HYdh0

- 파일로부터 입력 받기(테스트 및 디버깅때만 사용)
// 파일은 프로젝트 폴더에 저장
Scanner sc = new Scanner(new File("Input.txt"));	//	파일로부터 읽기 
BufferedReader br = new BufferedReader(new InputStreamReader(new FileInputStream("input.txt")));	//	파일로부터 읽기


- MM으로 코드 전송하는 방법

제일 앞의 ``` 세개는 키보드 esc 밑의 기호입니다
줄넘길때는 alt + enter 를 사용하면
전송이 아니라 줄넘김이 됩니다 (카톡처럼)
지원하는 언어를 우측상단에 적으면 해당 syntax가 반영됩니다

코드를 붙여넣고, 마지막에 다시 ```    세 번 적고 엔터쳐서 전송하면 됩니다
 
``` java(여기서 alt+enter)
for test_case in range(1, T + 1):
    astr = input()
    bstr = input()
    sol = 0  (여기서 alt+enter)
```
